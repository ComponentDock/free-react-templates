# Deploy templates to Surge.sh — one subdomain per template.
#
# Triggers:
#   1. A pull request is MERGED (manually by the user). The workflow figures out
#      which app folder(s) the PR changed and deploys each of them to
#      https://free-react-templates-<app>.surge.sh
#   2. Manual run (workflow_dispatch) — deploy a single app (input "app",
#      e.g. "aurora") or all apps (empty input).
#
# REQUIRED repository secrets (Settings > Secrets and variables > Actions):
#   SURGE_LOGIN — the Surge.sh account email
#   SURGE_TOKEN — from `npx surge token` (after `npx surge login`)
#
# The deploy itself runs `scripts/deploy-template.sh <app>`, which builds the
# workspace, adds the SPA 200.html fallback, and runs `npx surge` (headless,
# authenticated via the two secrets).

name: Deploy to Surge.sh

on:
  pull_request:
    types: [closed]
  workflow_dispatch:
    inputs:
      app:
        description: 'App folder to deploy (e.g. aurora). Leave empty to deploy all apps.'
        required: false
        type: string

permissions:
  contents: read
  pull-requests: read

jobs:
  deploy:
    name: Deploy templates to Surge.sh
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install fallow (release binary — not an npm dependency)
        run: |
          curl -fsSL -o /usr/local/bin/fallow https://github.com/fallow-rs/fallow/releases/latest/download/fallow-linux-x64-gnu
          chmod +x /usr/local/bin/fallow
          fallow --version

      - name: Determine apps to deploy
        id: apps
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ -n "${{ inputs.app }}" ]; then
              APPS="${{ inputs.app }}"
            else
              APPS="$(ls -d apps/*/ | xargs -n1 basename | tr '\n' ' ')"
            fi
          else
            # Changed files of the merged PR, via the GitHub API (robust even
            # if the PR branch was deleted after squash-merge).
            APPS="$(gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files" \
              --paginate --jq '.[].filename' \
              | sed -n 's|^apps/\([^/]*\)/.*|\1|p' \
              | sort -u \
              | tr '\n' ' ')"
          fi
          echo "APPS=${APPS}" >> "$GITHUB_ENV"
          echo "Detected app(s): ${APPS:-none}"

      # FAST_MODE merge gate: verify ONLY the changed apps (+ packages/ui if
      # touched) — the full gate is too slow at 1,000+ workspaces. Each app is
      # verified by scripts/verify-app.sh (typecheck + lint + knip + fallow +
      # 100%-coverage tests + build, ~2-3 min). The FULL gate (npm run gate)
      # still runs nightly (nightly-gate.yml), so repo-wide regressions cannot
      # go unnoticed.
      - name: Gate changed apps (PR merges only)
        if: github.event_name == 'pull_request' && env.APPS != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +e
          failed=""
          for app in $APPS; do
            echo "==> Gate: ${app}"
            if bash scripts/verify-app.sh "${app}"; then
              echo "==> OK: ${app}"
            else
              echo "==> GATE FAILED: ${app}"
              failed="${failed} ${app}"
            fi
          done
          # packages/ui changed? verify the shared package (typecheck + 100% coverage).
          if gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files" \
            --paginate --jq '.[].filename' | grep -q '^packages/ui/'; then
            echo "==> Gate: packages/ui"
            if npm run typecheck --workspace @free-react-templates/ui \
              && npx vitest run --root packages/ui \
                 --coverage.enabled=true --coverage.provider=v8 \
                 --coverage.include='src/**' \
                 --coverage.exclude='**/*.test.*' \
                 --coverage.thresholds.lines=100 --coverage.thresholds.functions=100 \
                 --coverage.thresholds.branches=100 --coverage.thresholds.statements=100; then
              echo "==> OK: packages/ui"
            else
              echo "==> GATE FAILED: packages/ui"
              failed="${failed} ui"
            fi
          fi
          if [ -n "$failed" ]; then
            echo "::error::Gate failed:${failed}"
            exit 1
          fi
          echo "Gate passed for: ${APPS}"

      - name: Deploy each template to its Surge subdomain
        if: env.APPS != ''
        env:
          SURGE_LOGIN: ${{ secrets.SURGE_LOGIN }}
          SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
        run: |
          # Deploy every app even if some fail; report failures and fail the
          # job only when at least one deploy failed.
          set +e
          failed=""
          for app in $APPS; do
            echo "==> Deploying ${app} to https://free-react-templates-${app}.surge.sh"
            if bash scripts/deploy-template.sh "${app}"; then
              echo "==> OK: ${app}"
            else
              echo "==> FAILED: ${app}"
              failed="${failed} ${app}"
            fi
          done
          if [ -n "$failed" ]; then
            echo "::error::One or more deploys failed:${failed}"
            exit 1
          fi
          echo "All deploys succeeded."

      - name: Summary
        run: |
          if [ -z "$APPS" ]; then
            echo "No template app changed in this event — nothing deployed."
          else
            echo "Deployed: ${APPS}"
          fi
