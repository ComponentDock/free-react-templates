# BtnSpark Development Outline & Design Notes

## Tasks / Todo Outline

1. Scaffold `apps/btnspark` using existing template structure.
2. Configure `vite.config.ts` with `injectUiSource()`.
3. Create sections:
   - `Navbar.tsx`: Title and navigation links.
   - `Hero.tsx`: Introduction and overview of button components.
   - `ButtonShowcase.tsx`: Sections for solid, outline, gradient, pill, icon, and loading buttons.
   - `Footer.tsx`: Copyright and Component Dock link (`https://www.componentdock.com/`).
4. Write tests for 100% coverage.
5. Register workspace in root `package.json` and ensure lockfile is synced.

## Fidelity Notes

- Source preview returned 404; designed faithfully based on standard professional button showcases and the screenshot layout showing modern clean UI buttons.
- Colors and typography match Tailwind 4 design guidelines.
