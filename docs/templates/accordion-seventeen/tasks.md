# Tasks & Design Notes: Accordion Seventeen

## Design Notes

- **Color Palette**: `#efefef` background, `#72c02c` green accent, `#212529` dark text.
- **Typography**: Roboto font family.
- **Interactivity**: Smooth collapsible accordion sections with Lucide icons (`Plus` / `Minus`) matching the ColorLib template-17 design.

## Task Breakdown

1. Scaffold `apps/accordion-seventeen` using simplest existing app structure (`apps/accordion-sixteen` or similar).
2. Configure `package.json` with workspace name `@free-react-templates/accordion-seventeen`.
3. Set up `public/CNAME` with `accordion-seventeen.free.componentdock.com`.
4. Implement sections (`Navbar` / header, `AccordionWidget`, `Footer`).
5. Write comprehensive unit tests in `src/components/Accordion.test.tsx` achieving 100% test coverage.
6. Verify build, lint, and test suite.
