# Spinwheel (Carousel 17) — Prep & Tasks

- Source: ColorLib Carousel 17 (`https://colorlib.com/wp/template/carousel-17/`)
- New Name: Spinwheel (`apps/spinwheel`, package `@free-react-templates/spinwheel`)
- Domain: Testimonial carousel landing / showcase

## Tasks

1. [ ] Scaffold `apps/spinwheel` (copy simplest app, update package.json name & homepage, add public/CNAME).
2. [ ] Implement components: `TestimonialCarousel.tsx`, `Footer.tsx`.
3. [ ] Implement `App.tsx` and styling in `src/index.css`.
4. [ ] Write unit & integration tests with 100% test coverage (`npm run test:coverage`).
5. [ ] Run per-app verification (`bash scripts/verify-app.sh spinwheel`).
6. [ ] PR and immediate squash merge.
