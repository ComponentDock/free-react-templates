/** 5px light-pink section divider (ColorLib Daren `.border_top`). */
export function BorderDivider() {
  return (
    <div
      data-testid="border-divider"
      aria-hidden="true"
      className="mx-auto h-[5px] w-[1110px] max-w-[90%] bg-brand-tint"
    />
  )
}
