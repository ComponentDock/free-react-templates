/* Shared brand button classes — recreated from the ColorLib "Bhost"
   design's `.button` (193×72 pill, radius 36px, uppercase 14px/700 white
   text with a 3-stop gradient hover sweep) and the solid gradient
   `domain_search_button` (193×70, radius 35px). */
export const btnGradient =
  'bg-brand-gradient inline-block rounded-full px-9 py-4 text-center text-sm font-bold ' +
  'uppercase tracking-wide text-white transition-transform duration-300 ' +
  'hover:-translate-y-0.5'

/* White pill button with a yellow hover — the pricing card "order plan"
   button (white, radius 36px, hover background #ffc016 + white text). */
export const btnWhitePill =
  'inline-block rounded-full bg-white px-9 py-4 text-center text-sm font-bold ' +
  'uppercase tracking-wide text-[#2c2c2c] transition-colors duration-300 ' +
  'hover:bg-gold-500 hover:text-white'
